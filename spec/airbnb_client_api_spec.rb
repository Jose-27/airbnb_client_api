require "spec_helper"

describe AirbnbClientApi do
  it "has a version number" do
    expect(AirbnbClientApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
